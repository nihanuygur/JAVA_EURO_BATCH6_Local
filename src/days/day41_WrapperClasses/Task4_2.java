package days.day41_WrapperClasses;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;

//public class Task4_2 {
//    public static void main(String[] args) {
//
//
//    }
//
//    public static void sortingSegments(String s) {
//        String alphabetic = "";
//        String numeric = "";
////        ArrayList<Character> list1 = new ArrayList<>();
////        ArrayList<Character> list2 = new ArrayList<>();
//
//        for (int i = 0; i < s.length(); i++) {
//            if (Character.isAlphabetic(s.charAt(i))) {
//                alphabetic += s.charAt(i);
//            } else if (Character.isDigit(s.charAt(i))) {
//                numeric += s.charAt(i);
//            }
//            ArrayList<Character> list1=new ArrayList<>();
////            list1=
//////            Arrays.sort(list1);
////            System.out.print(list1);
////            alphabetic="";
//
//            char [] list2=numeric.toCharArray();
////            Arrays.sort(list1);
//            System.out.print(list1);
//
//
//
//
//
//        }
//
//    }
    /*
      String str = "DCA";
        sortingSegments(str);

    }
    public static void sortingSegments(String s){
        String alphabetic ="";
        String numeric = "";
        ArrayList<Character> list1 = new ArrayList<>();
        ArrayList<Character> list2 = new ArrayList<>();

        for (int i = 0; i < s.length(); i++) {
             if(Character.isAlphabetic(s.charAt(i))){
                 numeric="";
                 list2.clear();
                 alphabetic+=s.charAt(i);
             }else if(Character.isDigit(s.charAt(i))){
                 alphabetic="";
                list1.clear();
                numeric+=s.charAt(i);
            }
             for (char c : alphabetic.toCharArray()){
                list1.add(c);
            }
            Collections.sort(list1);
            for (char c : list1){
                System.out.print(c);
            }for (char c2 : numeric.toCharArray()){
                 list2.add(c2);
            }
             Collections.sort(list2);
            for (char c2 : list2){
                alphabetic+=c2;
            }
            System.out.print(alphabetic);


        }

    }
     */

