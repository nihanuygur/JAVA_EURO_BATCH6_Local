package days.group1Tasks;

public class Registration {
    /*
    //////////  ENCAPSULATION    TASK  ///////////////

Create Registration Class in which you would have variables as email, userName and password that have an access scope
 only within its own class. (Every field should be declared as private)

After creating an object of the class user should be able to call methods and in each method separately pass values to
set users email, username and password. Create at least 2 objects and pass the values.

 Rules:  A. Valid email consider to be only gmail
              B. Valid userName and password cannot be empty and should have at least 6 characters.
   C. Valid password cannot contain userName.
 (You should print Error Messages )
     */

}
